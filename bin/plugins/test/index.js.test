
const command = (program) => {
    program
        .command('test')
        .description('Test command')
        .action(run);

}

const run = () => {
    console.log('Test command');

}

export default command;